# Laravel Blog Project (Blade)

مشروع مدونة باستخدام **Laravel 12** و **Blade Templates**.  
المشروع مقسوم إلى قسمين:

-   **لوحة الأدمن (Admin Panel):** لإدارة المدونات والفئات.
-   **واجهة المستخدم (Frontend):** لعرض المدونات، قراءتها، وإضافتها للمفضلة.

---

## ✨ المميزات

-   نظام علاقات many-to-many بين المدونات والفئات.
-   CRUD كامل للمدونات مع:
    -   إضافة / تعديل / حذف ناعم (Soft Delete).
    -   استرجاع من المحذوفات (Restore).
    -   حذف نهائي (Force Delete).
-   رفع الصور وتخزينها داخل `storage/app/public/blogs`.
-   CRUD للفئات (إضافة / تعديل / حذف).
-   واجهة مستخدم لعرض المدونات مع فلترة حسب الفئة.
-   نظام مفضلة (Favorites) للمستخدمين.
-   مصادقة باستخدام **Laravel Breeze**.
-   حماية لوحة الأدمن بميدل وير `admin`.

---

## 🛠️ المتطلبات

-   PHP >= 8.1
-   Composer
-   Node.js + npm
-   MySQL

---

## ⚙️ خطوات التشغيل (Setup)

بعد تنزيل المشروع من GitHub:

```bash
# تثبيت الاعتماديات
composer install

# نسخ ملف البيئة وضبط إعدادات قاعدة البيانات
cp .env.example .env

# توليد مفتاح التطبيق
php artisan key:generate

# إنشاء الجداول مع بيانات تجريبية (Seeder)
php artisan migrate --seed

# إنشاء رابط التخزين للصور
php artisan storage:link

# تثبيت مكتبات الواجهة وبناءها
npm install && npm run build

# تشغيل الخادم المحلي
php artisan serve



# تم إنشاء حساب أدمن تلقائيًا عبر Seeder:

Email: admin@example.com

Password: password

#  ملف SQL dump باسم:
database/sql_dump.sql


# يوجد صورة تجريبية يمكن تغييرها:
storage/app/public/blogs/sample.jpg

#مخطط تدفق الطلبات (Request Flow)
flowchart TD

A[المستخدم] --> B[إرسال طلب HTTP]
B --> C[Route في web.php]

C -->|Frontend| D[Frontend Controllers]
C -->|Favorites (auth)| E[FavoriteController]
C -->|Admin (auth + admin)| F[Admin Controllers]

F --> G[Form Request]
G --> H{authorize()}
H -->|ليس أدمن| I[403 Unauthorized]
H -->|أدمن| J[rules() Validation]

J -->|فشل التحقق| K[رجوع مع رسائل الأخطاء]
J -->|نجاح| L[تنفيذ الكود داخل Controller]

L --> M[Model + Database]
M --> N[استجابة HTTP (Redirect + رسالة نجاح)]

```
